class RouterComponent {
    /**
     * Retrives the callback associated to a request
     * 
     * @param {string} path
     * @param {string} method
     */
    handle(path, method) {
    };
}

module.exports = RouterComponent;