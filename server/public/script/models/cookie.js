class Cookie extends Circle {
    /** @type {Array.<Circle>} */
    chips = [];
    
    constructor(position, radius, stroke, fill, strokeWidth) {
        super(position, radius, stroke, fill, strokeWidth);
    }

    genChips(numChips, chipStroke, chipFill) {
        for(let i = 0; i < numChips; i++) {

        }
    }

    // draw(ctx) {
        
    // }
}